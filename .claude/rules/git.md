# Git Conventions

Project-independent git and commit conventions. These describe how to work with
version control, not what any particular project contains. They are meant to be
shared across repositories.

## Commits

Never add `Co-Authored-By` trailers to commits.
